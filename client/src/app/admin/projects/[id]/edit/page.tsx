'use client';
// Edit page re-uses the unified ProjectFormPage.
// The form auto-switches to edit mode when params.id is present and not "new".
export { default } from '@/app/admin/projects/new/page';
